version: "3.9"
services:
  psql_db:
    container_name: backend_agro
    image: postgres:14
    volumes:
      - psql-db-data:/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=backend_agro
    ports:
      - 5432:5432

volumes:
  psql-db-data:

networks:
  app-connect:
    driver: bridge